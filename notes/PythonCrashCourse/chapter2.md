- Python supports order of operations (BIDMAS)

- adding floats sometimes causes excessive decimal places

- if a float is used python defaults to returning a float

- when writing large numbers use _


